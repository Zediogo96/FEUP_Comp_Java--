import io;
import foo.Bar;
import comp.Table;

class Test extends Table {

    public static void main(String[] args) {
        this.foo();
    }

    public boolean foo() {
        int i = 0;
        int j = 10;
        if (i < j) {
            return true;
        }
        else {
            return false;
        }
    }
}
